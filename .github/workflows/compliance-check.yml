name: Compliance Check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  compliance:
    uses: CHITTYOS/chittyops/.github/workflows/reusable-compliance-check.yml@main
    with:
      service_name: 'shared'
      tier: 5
      check_health: false
      domain: 'none'
